# V80 LINE 廣告文案版型規則

## 對外格式

若來源明確含有 `正版授權`，每款文案固定依序輸出：

1. `正版授權`
2. 已移除 `新品` 字樣的商品名稱
3. `BGD-分頁代號-NO數字`
4. 可公開且不重複的商品資訊
5. 裝箱資訊
6. 10% 報價無條件進位後的售價與正確計價單位
7. `交貨2-3週`

沒有正版授權來源標記的商品不會自行增加 `正版授權`。

## 尺寸優先規則

- 若存在 `彩盒尺寸`、`包裝尺寸` 或 `端盒尺寸`，不顯示一般 `尺寸`／`產品尺寸`。
- 不同層級且數值不同的包裝尺寸可以同時保留。
- 數值相同的包裝尺寸只顯示一次；有 `彩盒尺寸` 時優先使用該名稱。
- `包裝:吊牌+獨立opp袋`、`包裝:12個/opp袋` 等是包裝方式，不是包裝尺寸，不會使產品尺寸消失。

## 既有規則維持

- LINE 文案不顯示重量、外箱尺寸、木架／木框及無木架成本備註。
- 售價取雲表 10% 報價並無條件進位。
- 未設定分頁代號、NO 不合法、計價單位缺失／不一致或價格不合法時停止產生。
- 所有規則只影響 LINE 廣告輸出，不修改採購解析與 Google Sheets 原資料。

## 驗收範例

```text
正版授權
三麗鷗家族系列輕享雙飲保溫杯530ml
BGD-G-1137
彩盒尺寸 7.6*7.6*23.8cm
帶鐳射標/4個顏色
材質:內316外304
裝箱 48個/箱
售價185元/個
交貨2-3週
```

一般產品尺寸 `7.4*7.4*22.8cm` 不應出現。

## 自動測試

```text
pytest -q -x
116 passed
```
