# V87.14 批次讀取與限流接續保存

## 改動

- 商品目錄仍涵蓋所有分頁與重複品號檢查，改以每 10 個分頁一組批次讀取 A:L（原本核對用的 12 欄）。重用同次操作的工作表控制物件，不快取來源值或雲端紀錄版本。
- 完整驗算及保存讀回成功後，沿用剛完成的結果，不因新增一個保存版本立刻再讀一次全批公式。重新載入、新連線及延遲保存恢復仍核實來源／公式／依據版本；部分重算與失敗列保留原本失效檢查。
- 保存前凍結內容、預期版本與唯一操作識別。HTTP 429 時保留全部結果，等待至少 60 秒（遵守更長 Retry-After），再先查雲端既有版本、必要時接續保存。最多自動接續 3 次並遞增等待，不重新驗算，也不直接重做 append。
- 已寫入但讀回失敗：使用相同操作識別與內容認出已保存紀錄，不新增重複紀錄。其他視窗改版、分段不完整、目的雲表變更不覆蓋，不自動當成功。
- 等待面板每 5 秒更新倒數；冷卻期間不建立雲表連線、不執行普通頁面讀取。非 429 或超過自動次數保留結果，提供下載備份、人工接續及明確放棄入口。
- 「處理完成」「保存待確認」「已保存讀回」分開顯示；有失敗列仍列出失敗，不宣稱全款通過。待保存資料只存在目前瀏覽器工作階段，重整／關閉可能遺失，面板明確提醒及提供備份。不是跨裝置持久重試佇列。
- 第二次檢查、當前問題登記、來源防變動、原圖、單位與防重檢查不省略。無自動核准、無商品價格變更、無 LINE 傳送。

## 驗證

- 完整本機回歸：960 passed、1 skipped（51.64 秒）。
- 新增 10 個測試：20 分頁目錄由 20 次資料讀取降為 2 次且商品／重複檢查一致、缺段阻擋、寫前 429、寫後讀回 429、冷卻零讀取、相同操作接續不重複、版本／目的雲表衝突、遞增等待與自動次數上限、即時保存不重讀、新工作階段版本失效及 UI 延遲恢復。
- UI 合成測試涵蓋 69 款結果完整保留、等待期間不建立 Store、自動恢復時間不變、等待時來源變更只使對應商品失效；未改商品列或核准狀態。
- 429 以本機故障注入驗證，不在正式雲表刻意耗盡額度。
- 減少請求不代表免除 Google 配額。其他工作階段／使用相同帳號的工具仍共用限額；持續限流會保留結果並停止自動重試。
- 正式站功能提交 `49ed373d59e2564613fb9acedaaecc8d1333565b` 已部署，並正常重新啟動載入 V87.14。透過正常 Streamlit UI 在既有檢視批次執行 17 款驗算，讀回保存成功、17 款表內一致、0 差異、0 無法重算；既有 17 項未解問題不變。本次執行未出現 429，未修改商品／核准／LINE 狀態。這是一次成功驗收，不是永不再限流的保證。
- 隨後按「重新載入雲端」，原驗算時間、17 款結果及 17 項未解問題均保留，未再按驗算；成功提示消失後仍能由雲端恢復原結果。
