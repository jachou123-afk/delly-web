# V82 發送管理第一版

日期：2026-09-14。運作於既有 Streamlit 網站；不另建應用程式。

## 操作流程

1. 切換「發送管理」，載入既有報價表，依日期／供應商／分頁篩選。最多 150 款一批，未選取商品需填排除原因，仍保留在批次內。
2. 填寫批次名稱、目標聊天室及核對人，建立雲端草稿。逐款加入原圖（JPG/PNG/WebP，每張 2 MB、每款 5 張內）、校對文案、儲存核對。可暫緩、重新載入單款來源、調整順序及修改草稿目標。
3. 確認整批：必要資料、圖文檢查、每款核對、同聊天室既有安排提醒及最新來源檢查通過後才能建立待發清單。缺號只是提示；真正對帳以固定清單的品號為準。
4. 在 LINE 進行實際發送，再回到管理區登記圖片／文案查驗結果。須輸入實際聊天室名稱、核對人、LINE 紀錄日期時間／依據。圖片已確認而文案未確認，只顯示部分完成。未提供自動傳送。
5. 中斷時登記「結果待確認」，核對清楚才能解除；重複登記相同部分會被阻擋。可記錄額外／重複發送異常及其處理依據。
6. 逐品號對帳，所有未完成、異常與不確定結果解決後，才可完成批次。可下載 UTF-8 CSV 清單。

## 儲存方式

- 復用現有 Google Sheets 連線。閱讀頁面不建立新工作表；首次保存才建立 `_發送批次`／`_發送圖片`，只寫這兩張專用表。
- 使用 RAW 追加資料，保存批次每次修訂、核對人、時間、查驗紀錄、排除原因；不覆蓋旧修訂。原圖以雜湊去重，原始位元完整保留，以小於單格上限的 JSON 分段存放；不在程式庫或 Streamlit 暫存磁碟永久存放私人图片。
- 分段數量及 SHA-256 驗證完整性；雲表欄位不符、讀取失敗、分段缺失、內容損毀、舊修訂、並行修訂皆停止。寫入後重讀驗證；逾時視為結果未知，要求重新載入，不能直接重試。
- 圖片採短索引定位與合併範圍讀取，批次確認時合併取得所需圖片；不逐款重新下載整張圖片表。原報價的讀取及重複檢查略過新增的紀錄表。

## 驗證

- 前版 394 項回歸通過，涵蓋現有解析、成本、校正、文案与跨供應商標準欄位檢查。
- 新增 44 項：領域流程、Google Sheets 替身儲存、真實 Streamlit 元件測試；完整執行合計 438 項通過。包含切換日期時更新自動批次名稱，並保留使用者自訂名稱。
- 直接覆蓋 no1126 → no1130 的漏發情境：1127、1128、1129 留在未完成清單，禁止結案。
- 驗證日期／供應商篩選的缺號提示不強行補商品、未知代號及重複 NO 不被悄悄丟棄、完整排除紀錄。
- 驗證變更圖片／文案後重新核對、未存編輯禁止整批確認、來源變更或搬列阻擋確認、已確認後內容不能悄改。
- 驗證目標聊天室各自計算、重發原因、部分完成、未知結果、逐款對帳、同部分重複登記與異常處理留痕。
- 驗證重新建立儲存物件可還原批次與原圖、報價列不變、原圖去重、資料／圖片校驗、逾時後查回、舊視窗寫入及並行衝突阻擋。
- 瀏覽器用本機 `tools/preview_dispatch.py` 合成資料查看桌面圖文並排、確認批次後仍為 0/5 完成。此預覽檔留於工作區外層，不部署正式站。
- 正式原 Streamlit 網址已顯示 V82，發送管理與商品載入成功。9/12 日期篩選列出 68 款，1127～1129 皆在清單；本次僅唯讀驗收，未建立真實批次或登記已發。部分舊商品的單位檢查提醒不代表已完成原文核對。

## 第一版限制

- 「已確認」是核對人的 LINE 畫面查驗紀錄，非 LINE API 回執；不自動讀取 LINE 全歷史、浮動圖片或推送訊息，也未自動匯入此前已發 20 款。
- 圖片與原文的語意配對仍須人工核對。共用欄位驗證通過不代表每家供應商真實格式／整批商品已驗收。未知商品分頁代號保持阻擋，不猜分類代碼。
- 確認後內容鎖定；需大幅改動已確認批次時，另建立草稿並標明再次安排原因。相同聊天室名稱由操作者核對群組成員，本版無 LINE 群組 ID。
- Sheets 不提供跨視窗 compare-and-swap 交易锁；本版偵測同一批次修訂衝突後停止，不自動合併。不同批次同商品的提醒不是全域交易鎖。同一批次請由一位操作者處理。
- 雲端保存可跨重整／裝置繼續，但紀錄與圖片仍受 Google Sheets 容量和 API 配額限制；大量長期使用需後續移轉專用儲存。首次真實保存與 LINE 實際送出不在隔離測試範圍。
