# V81 跨廠商共用防呆（本機候選版）

日期：2026-09-14。尚未部署正式 Streamlit 網站，也未改寫雲表或發送 LINE。

## 設計邊界

- 保留既有 `parse_text` / `parse_text_legacy` 的逐欄與同行格式辨識，沒有以多品村格式取代其他來源格式。
- 共用檢查作用於解析後／雲表的標準欄位；廠商由操作者明確選擇，不從貨號前綴或排版猜測。
- 原有包郵判斷、重量、運費、匯率及報價公式未改寫；多品村包郵仍只適用其明確廠商值。
- 已有未知欄位、单位不明與費用歧義的阻擋規則保持不變。解析不明時應補來源格式樣本，不套另一家的數字。
- 不改 Google Sheets 的區塊格式、欄位、圖片、NO、日期或新增稽核工作表。

## 本次防呆

1. 原文每次變更，商品草稿編號遞增；清除上一款手動校正、名稱／貨號編輯、操作方式、分頁、廠商及確認。即使 A → B → A，也不復用 A 的舊草稿。
2. 新增商品的廠商／分頁預設空白；未選定不可新增。原位修正仍須明確選定 NO，之後顯示原記錄的廠商；不完整商品依舊只能清除不安全衍生數字。
3. 選定廣告商品後，程式先檢查進價、計費重量、兩項運費、成本、10% 報價及廠商。進價／重量／成本須為正，運費可為零但不可缺失或為負。10% 報價須等於 `ROUND(成本 / 0.9, 1)`。
4. 阻擋零裝箱量、格式不合法的數字，以及已有的 NO／單位／分頁代號錯誤。`52,9` 不會被猜成 `529`。
5. 檢查通過後才開放人工原文／原圖核對。按「檢查並產生 LINE 文案」時重新讀取該雲表分頁，不使用清單快取；NO 重複、移動、刪除或區塊資料改變即停止。
6. 已知內部欄位逐段排除：同一行的授權、重量、外箱或木架資訊不再連帶刪除公開材質／包裝。附加費用確認的稽核備註整行不對外顯示。

## 驗證範圍

`pytest -q -x`：394 項通過（含真實 Streamlit 元件測試；Google 連線與寫入均使用替身）。

| 範圍 | 覆蓋與限制 |
| --- | --- |
| 多品村既有格式 | 逐欄、單重／箱重、個／盒、木架及費用歧義等既有回歸測試 |
| 菲凡既有格式 | 貨號、價錢、装箱與箱重同行的已見格式；並測換款後實際存檔公式 |
| 跨廠商包郵隔離 | 三種已知欄位形狀 × 六種廠商值，共 18 組；不從格式猜廠商 |
| 優娜卡樂星／自訂廠商 | 僅驗證廠商值不會誤套多品村包郵；未取得真實原文，不代表來源格式已驗收 |
| 共用防呆 | 既有 327 項 + 新增 67 項；缺值、混合欄位、数字格式、重選、快取變動等 |

`tests/test_supplier_compatibility.py` 使用去識別化／合成格式案例，並非新增供應商商品的逐款核對結果。既有私人原文檔留在工作區外層，不複製進程式庫。

## 尚未解決／上線前注意

- 程式的一致性檢查不等於原文真實性驗證，不可因此把 9/12 全批商品標成已核對。
- 本版不自動從供應商原文對比每一個雲表欄位，也不自動驗證原圖。這些仍須逐款核對。
- 成本與報價自洽不代表採用匯率／運費是目前應採用的版本；舊公式內嵌的參數仍須確認。本版不批次改價。
- 重新讀取是產生文案當下的檢查，不是跨使用者交易鎖，也不能使已複製或已發送的文案自動更新。
- 尚未實作持久的原文核對憑證、圖文配對紀錄或 LINE 發送狀態／防重送；網站本身只產生文案。
- 正式上線前應補優娜卡樂星及其他實際使用廠商的代表原文與預期值，並確認最新正式程式差異；不可將本地舊基底直接覆蓋遠端的新修改。
- 上線後須在網站確認 V81，使用各家樣本演練（不寫入正式資料／不發 LINE），包括缺成本與切換廠商情境。

## 舊資料相容性

格式解析沒有被替換；但舊資料若缺廠商、成本、運費等必要值，或報價與成本不一致，會被新檢查擋住。這是刻意要求補核對，程式不會自行修正舊資料或猜補數值。
