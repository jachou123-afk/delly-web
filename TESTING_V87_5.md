# V87.5 直接看圖與維護功能收納

## 使用方式

- 選擇既有廣告批次，即展開商品圖片。每頁 6 款、三欄卡片；不需要勾選載入。
- 可依品號／品名搜尋，使用上一頁、下一頁或頁碼切換。圖片右上角可放大。
- 文案收在每款「查看文案」；成本驗算與人工核對保留於圖片區下方。
- 載入失敗時顯示「重新載入圖片」；真正缺圖及多張待確認候選仍明確標示。
- 原圖搬移與縮圖管理收至側邊維護設定，預設不執行。連線測試也需明確開啟。

## 圖片處理

優先讀已存在的 NAS 預覽副本；沒有副本時，逐張讀取確切配對原圖並在記憶體產生
暫時預覽。看圖不建立雲端檔案或索引。既有副本損壞、索引衝突、跨圖庫及設定錯誤
仍停止，不替換商品圖。原圖下載、配對、核對、LINE 發送與報價公式保持原邏輯。
既有預覽快取 48 張／8 MiB、原圖快取 5 張／16 MiB 及每頁最多 30 張限制保留。

## 自動驗證

- `pytest -q -rs`：824 passed，1 skipped。略過項目依原測試規則需要未公開的私人圖片素材。
- 69 款／63 張跨 12 頁全部遍歷、首次自動載入、快取重用、讀取失敗後重試、
  搜尋與空結果、頁碼邊界、缺圖、排除項目均驗證。
- 缺預覽副本的舊原圖與已移至 NAS 的原圖都可讀取，前後雲表、NAS 物件、批次、
  配對及人工核對狀態一致；保留損壞副本與異常索引的防呆測試。
- 維護工具預設不出現在主頁，不啟動維護連線；開啟後仍可執行既有明確操作。
- 合成圖片已於瀏覽器檢視三欄卡片、圖片自動顯示與內建放大按鈕。

## 部署

以 GitHub main `b1dfbfdf9ce7bfd917e3a6aab4488311f1da6b87` 為基底。
requirements 僅更新發版註記以觸發 Streamlit 完整重建，套件版本不改。

`bd93189beaf2767f88d08735b1a763b3f5db72ad` 已推送並完成正式站驗收：

- 頁首 V87.5，連線及圖片維護設定收在側邊，主頁沒有搬移、縮圖進度或載入勾選。
- 開啟既有草稿：69 款、63 款有配對图片、6 款待補圖／確認配對，商品圖片預設展開。
- 第一頁 3 張圖片自動顯示、3 款仍提示待配對；下一頁正確切換第 7～12 款，6 張均顯示。
- 實際點第二頁第一張圖片的放大按鈕，商品圖正常全螢幕顯示，關閉後回到清單。
- 線上抽驗第 1、2 頁，不宣稱線上逐頁全驗。全 12 頁另有本機合成資料測試。
- 未保存或確認真實批次、未改商品資料、未搬移或上傳圖片、未發 LINE。
