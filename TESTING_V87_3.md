# V87.3 NAS 商品原圖與分批對帳

## 範圍與保護

- 新增 `NasDispatchStore`，沿用原報價／廠商格式／商品配對／人工核對／LINE 邏輯。
- 獨立 `_商品圖片位置` 只保存小型、可驗證的 NAS 位置索引；不包含帳密、SID、
  主機網址或 base64 原圖。使用獨立 A:B 列索引，只讀所需的 metadata 範圍。
- 有 NAS 索引必須讀 NAS；離線、檔案遺失／損壞、跨圖庫、衝突、設定消失均報錯，
  不會暗中回讀舊 payload。沒有 NAS 索引的舊圖仍可讀取。
- 配置 NAS 後新圖直接存 NAS；沒有配置的其他部署仍保留原儲存流程。
- 每張上傳 skip、讀回驗證原檔後，才在 10 張一組的範圍發布並讀回核對索引。
  索引不可變；相同內容重複寫入可讀回辨識，位置衝突不覆蓋。
- 搬移只針對已綁定、舊原檔仍存在的 hash，每次最多 10 張；不搬未唯一配對圖，
  不移除舊 payload、不改商品配對、價格或批次狀態。NAS 新圖不會誤列為待搬舊圖。
- 最後另分批重新登入，從 NAS 讀回每張原圖，逐位元組與舊檔比對；介面列出檔名、
  大小、校驗碼及結果。完成數不以圖片位置索引數冒充實際對帳數。
- 網頁沒有開啟「讀取 NAS 搬移進度」時不查搬移進度或執行搬圖。
- 原圖快取在本次搬移後清除；其他既存工作階段若已有原檔快取，應重新載入再驗收。

## 本機與線上驗收

本機新增：63 張／69 款合成範例分 7 批搬移，逐張讀回，所有既有雲表逐格不變；
混合新舊圖讀取、NAS 新圖保存、設定未配置／格式錯誤／消失、損壞、跨圖庫、
索引衝突、寫後逾時、明細 UI 與完整發送入口接線測試。全部使用合成資料與假 HTTP。

2026-09-15 完整回歸：`pytest -q -x`，795 項通過（42.40 秒）。

### 實際線上驗收（2026-09-15 已完成）

- `6576e2e` 已部署到原工具，線上標題 V87.3 與 NAS 連線測試通過均已確認。
- 實際 63 張原圖分 7 批（10／10／10／10／10／10／3）搬移，寫後校驗再發布索引；
  介面確認已切換 63 張、待搬 0 張，舊圖備份保留。
- 另行 7 批開新 NAS 連線下載，每張與舊原檔逐位元組比對；63／63 張及 SHA-256
  全部一致，總計 13,899,638 bytes。不是只以位置索引筆數當成讀回成功。
- 重新載入網頁後實際載入整批原圖，DOM 共有 63 個圖片元素，全部成功解碼，
  未出現圖片讀取失敗訊息；另目視確認商品圖與對應文案並列。
- 原草稿維持 69 款、63 款可帶圖、已選 0 款；6 款待補／確認配圖仍保留。
  未保存人工核對、未發 LINE、未改報價或商品配對。
- 讀回對帳計數是當次工作階段紀錄；重開頁面歸零不代表 NAS 原檔被刪除。

## 尚未包含

縮圖／分頁、記憶體快取上限及整份原表 XLSX 匯出 403 修復仍不在本批範圍。
舊原圖仍保留於雲表作備份，故本批不會自動縮小原雲表或消除既有匯出限制。
其中縮圖、分頁與下載圖片快取上限已於後續 [V87.4](TESTING_V87_4.md) 完成；
整份 XLSX 匯出限制仍待處理。
