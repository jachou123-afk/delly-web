# V87.8 整批確認與例外處理驗證

日期：2026-09-15。基底：`caac57d54d488bb21b45d80409c39e9322123edb`，原儲存庫 `jachou123-afk/delly-web` 的 `main`。

## 使用者授權範圍

簡化「③ 單款明細與人工核對」之後的準備操作：正常商品不用逐款勾選、輸入核對人或保存核對；只有异常／修改才展開單款工具。最後整批明確確認一次，不自動發 LINE。

## 實作

- 單款修改、原文／參數／算式預設收起；多選驗算與每頁 6 款圖片預覽保留。
- 已存圖庫的正確身分配對可直接列入待確認候選，最後確認時保存於本批；未存圖包候選不自動採用，既有本批圖片優先。
- `batch_confirmation` 保存確認人、時間、內容摘要及缺原文商品 IDs；不生成假的逐款 `review`。
- 缺原文可在最後同一個確認勾選中知悉限制。仍保持 `source_ready=False`，不把表內重算當成廠商原文核對。
- 缺圖、超過 5 圖、未知計價單位、文案價格／裝箱不一致、內部成本外洩、計算未通過、失效原始依據、來源變動、重複安排未填原因均阻擋。
- 確認前重讀雲端成本依據、圖片配對及原檔；原檔每次最多 5 張以限制記憶體。最後重新讀取商品來源和批次歷史，再由原本樂觀版本控制保存。
- 舊嚴格 domain 入口的預設保護保留，已確認批次與 LINE 畫面結果登記／對帳不變。

## 測試

- 最終完整回歸：857 passed，1 skipped，45.25 秒（私人本機圖片素材不公開）。
- 新增 20 個合成案例，含 69 款唯讀帶圖／驗算、一次整批確認、不偽造原文或個別核對、LINE 完成數仍 0、未勾整批不可保存。
- 確認前公式改動、原文刪除、圖片配對替換、原檔損壞、來源變動均拒絕且不保存批次。
- 既有 UI 測試依新流程更新，保留單位修正、來源變動、補原文、未存文案修改、圖片唯一配對、搜尋多選與目標聊天室保障。

## 發布／線上驗收

- 程式提交 `4423b71790d3a407876e5eca7a11054e69431800` 已推送原 main；GitHub API 回讀相同 SHA，本機與 origin/main 相同且乾淨。
- 正式站 `https://delly-web-eczbucrrkrr7caoe568w4w.streamlit.app/` 已確認 V87.8。
- 實際畫面檢查：「③ 處理異常與最後確認」可見，單款修改與順序工具預設收合；最後整批勾選會顯示缺原文數量。圖片維護仍收於側欄。
- 2026-09-15 15:15:30 +08:00 唯讀驗算 69 款，69 表內重算一致，0 差異，0 無法完成；原文已保存 2、缺 67。這是當時線上資料狀態，使用者可繼續操作而改變數量。
- 新確認流程顯示 10 款待整批確認、59 款需處理、0 排除；仍有計價單位未確認與缺圖，沒有自動忽略。批次仍為草稿，最終確認保持未勾選／停用。
- 首次線上試算遭 Google Sheets 每分鐘每使用者讀取配額 429；停止連續讀取並等待正常冷卻，再重新載入／重試一次後取得完整 69 款結果。沒有更改服務配額、權限或繞過限制。
- 正式資料沒有在本次驗收中保存批次核對、修改價格／圖片配對或發送 LINE。一次確認保存／防競態與失敗保護由合成測試驗證，沒有拿真實批次試按确认。
