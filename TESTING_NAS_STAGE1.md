# NAS 圖庫第一段：讀寫基礎（尚未切換商品儲存）

## 目前範圍

`synology_image_store.py` 是可注入測試連線的獨立 Synology File Station
原圖儲存元件。尚未接入 `get_dispatch_store()`，沒有搬移原圖、
修改 Google Sheet 圖片索引或更動 Synology Drive 同步任務。
V87.2 只以本元件進行固定合成圖的部署檢查，詳見 `TESTING_V87_2.md`。
專用 NAS 共用資料夾及非管理員帳號另經使用者同意後建立並讀回核對；
真實主機、帳號及權限紀錄只保留於私人工作區。

原有 V87.1 圖庫、核對、報價及發送行為保持不變。

## 已實作的保護

- 使用 HTTPS 且固定開啟憑證驗證，不跟隨任何重新導向。
- 帳密及 SID 僅放在 POST 內容；不放圖片 URL、索引或錯誤訊息。
- 先以無帳密 API 查詢確認能力與相對路徑，再登入專用帳號。
- 僅接受獨立共用資料夾，不接受根目錄、home、homes 或整個圖片區。
- 每個連線工作階段第一次上傳前，檢查該精確圖庫路徑存在。
- 檔案路徑由 SHA-256 和已驗證的格式產生，不使用外來檔名作路徑。
- 檔案存於 `指定圖庫/originals/雜湊前兩碼/完整雜湊.副檔名`。
- 保留原圖位元組，不更換、刪除或覆蓋同名檔案。既存檔案也重新核對。
- 上傳後重新下載驗證大小、SHA-256 及圖片格式，才回傳小型索引。
- 上傳逾時屬「結果待確認」，不自動重送或聲稱成功。
- 回應採分段讀取並限制大小。HTML、API 錯誤與下載失敗不當成缺圖。
- 帳號驗證不自動重試、不關閉兩步驟驗證、不讀取管理員瀏覽器工作階段。
- 工作階段結束只登出自己的 SID，不影響 DSM 瀏覽器登入。
- 沿用現有 2 MB／2500 萬像素原圖限制，未順便放寬或壓縮舊圖。

## 驗證界線

單元測試只使用合成 PNG 與記憶體中的 HTTP 模擬服務。涵蓋原圖往返、
相同圖片重試、錯誤索引、跨圖庫路徑、上傳結果不明、損壞檔案、超量回應、
登入失敗、惡意 API 路徑、TLS 失敗、重新導向與敏感錯誤訊息遮蔽。

實際 NAS 只做過不含帳密的 `SYNO.API.Info` GET／POST 探測。
該探測在操作電腦端成功回傳 Auth、List、Upload、Download API 能力；
它**不等於**專用帳號已建立、已有讀寫權限，或 Streamlit Cloud 已完成串接。
完整的 NAS 主機、IP、帳號密碼不放入公開程式庫。

2026-09-15：新增元件 45 項測試通過，完整回歸 **741 項通過（49.36 秒）**。
第一次完整執行曾在既有 `test_batch_cost_ui` 啟動時超過其 15 秒限制；
原測試單獨重跑通過，再次完整執行亦通過。未延長逾時或修改既有測試。

## 啟用前仍須完成

1. 已取得獨立 NAS 圖庫及專用非管理員帳號的具體授權。
2. 使用者已自行設定新密碼；已讀回確認此帳號無其他共用資料夾存取權。
3. 使用者已回覆完成 Streamlit 私密設定；實際可用性仍待部署檢查，未讀出 Secrets。
4. 從實際 Streamlit 執行環境確認登入、根路徑與一張合成測試圖的寫後讀回。
5. 接入獨立小型圖片位置索引；保留舊 base64 圖片作遷移前備份。
   只有沒有 NAS 索引的舊圖才讀舊圖庫；NAS 讀取失敗不得暗中退回別張圖。
6. 分批遷移現有唯一配對圖片，核對數量與雜湊後才發布每張索引。
   不改商品身分、價格、人工核對狀態或 LINE 發送狀態。
7. 再加入縮圖與分頁／按需載入；本階段尚未解決整批原圖記憶體累積問題。

QuickConnect 入口本次可用仍需實際驗證穩定性。若入口變更，程式停止並由
使用者確認新網址，不自動追蹤轉向、不開放新的路由器埠或公開圖庫。

## 官方依據

[Synology File Station API](https://global.download.synology.com/download/Document/Software/DeveloperGuide/Package/FileStation/All/enu/Synology_File_Station_API_Guide.pdf)：
API 查詢、POST 登入、上傳 skip、下載與登出契約。
