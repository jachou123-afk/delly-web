# V87.1 整批圖片預覽漏接修正

## 已確認原因

V87 的單款明細與圖庫可自動帶圖，但「整批廣告預覽」仍只讀取批次 `item.images`。僅保存到共用圖庫、尚未保存本批核對時，這個欄位仍為空，因此整批區錯誤顯示已加入 0 張。不是原圖匯入失敗，也不能用自動保存核對狀態掩蓋問題。

## 修正

- 草稿整批預覽接入同一份已驗證的圖庫對應，優先保留本批既有圖片；未保存者可顯示圖庫圖片或單一未保存候選，標明來源與未核對狀態。
- 多張未保存候選不猜選；已排除款保留名稱／原因但不下載；無可用配對款仍保留商品列與警告。
- 原圖下載仍需勾選「載入整批商品圖片」，預設不下載整包；未勾選時也顯示真實可預覽數量與操作提示，不再顯示含糊的 0 張。
- 同一組圖片只下載一次，每次最多 10 張一組讀取，沿用原檔校驗。部分讀取失敗逐款報錯，不當缺圖、不換其他圖片。顯示內容不寫雲表、不保存本批圖片或人工核對。
- 上方選取清單區分「圖庫已存」「本批已存」「候選」及「待配對／補圖」，此清單仍是張數欄，不是縮圖欄。
- 已確認／發送中的批次仍只用鎖定的本批圖片；本修正只適用草稿預覽。

## 驗收範圍

- 全套 `pytest -q -x`：696 項通過（54.77 秒），包含既有 690 項回歸；沒有以測試數冒充商品數。
- 新增 6 項測試，包含 69 款／63 張圖庫圖片的真正 image 元件數（不是只驗證 caption）、6 款待配對保留、7 次分組讀取、重新開關不重複下載、原表及批次完整不變。
- 讀取首組失敗時，10 款明確報錯，其餘 53 張顯示，69 款文案仍保留；已排除款不下載；本批圖片優先、多圖候選不猜、完整發送頁入口實際接線。
- 原先測試遺漏整批預覽場景，本次加入完整頁面接線回歸，不能再只以單款預覽成功宣稱整批功能完成。
- 分類設定已依使用者明確確認，在原工具保存 W玩具=W、W娃娃=WA、D吊飾=D；讀回五組設定正確，G/S 未改。
