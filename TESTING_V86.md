# V86 多選與整批驗算

## 範圍

- 原發送管理草稿加入原生多選、全選本批、取消全選及明確選取數量。全選包含搜尋未顯示與已暫緩的商品；搜尋不取消既有選取。
- 勾選範圍與單款明細分開；點品號／商品只同步明細選單，下一次操作仍保留焦點。
- 驗算所選商品後，每款恰好一列，列出原成本／重算成本／差額、原售價／重算售價／差額、計算結果及來源待處理事項。
- 沿用獨立 Decimal 驗算與既有公式檢查，不重寫廠商解析、成本公式或名稱正規化。
- 廣告尚未產生時顯示「原廣告售價：尚未產生」並附來源／文案阻擋原因，不把缺少廣告價格誤稱為原表漏填，也不自行補未確認的分類代碼。
- 只讀取，不自動保存來源依據、核對狀態、價格、圖片、文案、發送或對帳紀錄。

## 防漏與失效保護

- 以穩定商品 ID 保存勾選，不以篩選／排序後的位置當商品 ID。
- 每次先重讀來源目錄；品號不唯一、來源已刪除／修改／移位、讀取失敗均保留結果列，不能判為通過。
- 每段最多 20 款，分別讀取顯示值、公式及再次顯示值；前後來源 hash 必須符合。69 款同分類使用 12 次分段讀取；目錄與來源依據另讀。
- 缺少完整廠商原文／當次參數會明列待補資料；計算一致不等於人工核對完成。
- 草稿修訂／來源／順序／排除變更使舊結果失效；重新載入雲端清除結果。僅更改選取時，明確標記下表為上次範圍，需重新驗算。
- 最終逐款與整批確認依然使用既有重讀及鎖定機制，不以本次畫面快照取代確認時檢查。

## 自動化驗證

- `pytest -q -x`：最終 653 項通過（32.04 秒），含既有 632 項回歸與本次新增 21 項。
- 69 款合成資料完整結果、子集、排除商品、來源變動／缺失／重複、讀取前後變動、局部分段讀取失敗、缺公式／重量、錯成本、缺來源依據、回應漏列及非法範圍。
- 真實 Streamlit AppTest 驗證全選／清除的原生選取狀態、搜尋隱藏選取、結果 69 列、範圍變動提示、重載清除、單款明細選單的程式同步與後續 rerun。
- 合成測試讀寫前後的替身雲表完全相同，可發送仍為 0，整批確認維持禁用。

## 操作驗收與限制

- 本機瀏覽器使用隔離合成資料，不連正式 Sheets。已檢查全選 69 款、取消單款剩 68 款、取消全選、從空白連續勾兩款、品號切換及執行後結果 69 列。明細維持第三款，沒有因勾選／執行跳回第一款。
- 瀏覽器驗收找出並修正舊明細選單前端值在下一次操作覆蓋焦點的問題；用更新掛載元件值取代刪除元件狀態，新增回歸測試。
- 結果僅保存在目前瀏覽器連線記憶體；重新整理頁面／重新連線需要重新選取及驗算，不冒充持久稽核紀錄。
- 同時編輯原表仍存在讀取後變動的時間窗，結果標示驗算時間；後續確認前必須重讀。不得以本功能宣稱所有來源格式或真實商品已人工核對。
- `c249987`、`46521f2` 已推送原 main。正式站已確認 V86 及三個分區，既有草稿全選 69 款後只讀驗算得到 69 列：66 計算一致、0 數值差異、3 廣告資料尚未產生而待處理；未把 3 款漏掉或直接判為通過。
- 正式可發送仍為 0，需處理仍 69，未儲存來源／核對或發 LINE。66 計算一致不等於已確認供應商原始資料。最後提示修正尚待正式站複驗。文件不包含正式商品成本或廠商原文。
