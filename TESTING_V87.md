# V87 共用分類設定與商品原圖庫

## 範圍與操作

- 在原發送管理加入「分類代碼設定（全工具共用）」；報價、唯讀文案及發送管理共用同一份雲端設定。既有 G/S 代碼固定，新增代碼不可重複，既有分類不能直接改碼。W玩具、W娃娃、D吊飾須經操作者明確設定，程式不按首字猜碼。
- 舊商品從「圖片管理」匯入 ZIP／原表 Excel，先列出每款配對結果，再明確保存唯一候選。ZIP 貨號比對整份目錄，跨分類／廠商重複不自動配對。多張候選須逐款選定。
- 商品原圖保存於既有 `_發送圖片`，以新增 `_商品圖片對應` 記錄商品身分與圖片雜湊；圖片不放入公開程式庫。分類設定另存 `_分類代碼`。不更動產品 A:L、原表 M:T 圖片、批次核對或 LINE 狀態。
- 草稿先讀取輕量對應索引，只下載目前查看商品的原圖；已綁定的 1～5 張圖片可自動帶入。原圖仍須人工確認是否同款，帶圖不代表已核對。
- 新報價可選填本款原圖。商品寫入成功且再讀內容一致後才保存圖片。商品成功但圖庫失敗，明確提示只補圖片，不重複新增商品。

## 防錯與限制

- 身分包含分類、NO、品名、供應商貨號及正規化供應商。改名／改貨號／換供應商暫停自動帶旧圖；價格、日期與列位改動不丟失圖片綁定。
- 寫入前後重讀商品和圖片對應版本；來源已變、NO 重複、他人已修改、圖片损壞或讀取失敗都不當成成功。圖庫讀取失敗不顯示為 0 張。
- 圖片以原始位元組保存，校驗雜湊並去重；不生成／壓縮／猜圖。單張最多 2 MB，每款最多 5 張，每次最多 150 款。
- 批次原圖分段 append，單一圖片紀錄不跨寫入请求；所有圖片寫後驗證通過，再發布小型對應記錄。失敗可能留下尚未綁定但可再利用的原圖，不觸發商品重寫。
- 舊綁定不自動覆蓋；逐款明確確認替換需符合讀取版本，保留舊圖和綁定歷史。並行分支衝突時停止，不提供「最後寫入者勝出」假成功。
- 原表 Excel 匯出仍可能受整份表過大限制；ZIP 是替代匯入入口，不聲稱已修復 Google 匯出限制。
- Sheets 不提供跨圖片／對應／產品表交易。寫入前後讀取縮小但不能消除並行變動時間窗；發送確認仍沿用原有即時重讀與人工核對。
- 無照片的舊商品／沒有唯一命名的圖片仍需补圖或確認。分類設定只新增對應，不追溯改寫歷史廣告／已發品號。

## 驗證

- 最終 `pytest -q -x`：690 項通過，36.57 秒；既有 653 項回歸與新增 37 項。
- 合成資料驗證 69 款完整持久化、原始位元組一致、相同檔案去重、反覆匯入冪等、5 張已選圖綁定、6 張阻擋、全目錄重複貨號阻擋。
- 覆蓋價格／身分改變、來源在上傳中變動、舊版本替換、服務端成功但回應中斷、資產／映射損壞及版本衝突；報價表、核對及 LINE 狀態保持不變。
- Streamlit AppTest 驗證自動帶入／僅讀取目前圖片、唯一候選整包保存、多个候選預設不選、明確多選保存、重載後自動帶圖、選填報價圖片與分類設定。
- 本機瀏覽器以隔離合成 69 款驗收：2 款有候選，其中 1 款唯一、1 款多張；保存唯一圖片後顯示 1／69 自動帶圖，重新載入仍顯示已綁定原圖；核對未勾選，建立待發清單仍禁用。
- `831e215` 與 `8593176` 已推送原 main，正式站確認 V87、分類設定及圖片管理入口。
- 使用者明確授權舊原圖 ZIP 匯入原工具，僅保存唯一配對，不改價格、不標成已核對、不發 LINE。正式 69 款草稿預覽得到 63 款唯一配對；點保存後，寫後對帳為 63 款已綁定、0 款寫入待處理，圖庫顯示 63／69。
- 原包 65 個檔案中，1 張超過既有 2 MB 單張上限，1 張檔名無全目錄唯一對應；未擅自壓縮、改名或猜配。其餘 6 款商品仍完整保留在配對表中，均顯示未找到圖片。
- 本檔不包含正式商品成本、供應商原文、檔名或圖片。實際未配對品號留於私人交接紀錄；分類新增值仍待使用者確認，未擅自保存。
