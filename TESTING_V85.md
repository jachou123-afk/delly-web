# V85 供應商名稱統一

## 範圍

- 使用者要求小寫 `v`＋廠商名，並明確確認四組既有別名合併；規則集中於 `supplier_names.py`。
- 新增／修正商品的下拉選項和寫入區塊使用正規名稱。新增商品仍須操作者明確選擇供應商，不依貨號或原文猜測。
- 發送管理的篩選與摘要以正規名稱分組，但原始商品 block、source_hash、歷史批次與核對記錄保持原樣；顯示合併不掩蓋來源變更。
- 報價公式與獨立驗算只共用名稱辨識，數值計算仍分開。只有已確認為多品村的精確名称套用既有包郵政策，其他供應商維持原費率。
- 正式雲表名稱清理由受限 L 欄、排除公式、完整字串比對執行；未改價格、公式、圖片、文案或歷史發送資料。私有逐格復原對照與完成紀錄留在工作區外層，不加入公開程式庫。

## 驗證

- `python -m pytest -q -x`：632 項通過（29.17 秒），含原有 536 項。
- 新增大小寫／前綴分隔／全形、四組別名、重複執行、未知廠商保留、精確包郵、六列寫入、舊資料下拉預選、不自動保存、篩選合併不漏商品、來源變更保護測試。
- `git diff --check` 通過。
- 正式雲表更名後 API 全量逐格比對目標新值及非目標 L 欄內容與格式；6 個更動分頁均檢查原生畫面代表位置。部分原有浮動圖片遮住 L 欄尾端，名稱在公式列完整可見；不擅自重排原圖。
- 程式部署待推送後正式站讀取驗收；不得僅依版本標題推論新模組已載入。

## 邊界

- 不模糊比對廠商，不新增未經確認的別名關係，不修改供應商原始訊息。
- 雲表舊有錯列的供應商文字只原位改名，不搬移、補填或合併列；空白供應商不猜測。
- 人工直接在 Google Sheets 輸入仍可產生新寫法；工具可正規化其識別的名稱，並不是鎖住雲表。未知異名需要另行確認。
- 更名後舊草稿可能提示來源變更；保留重新核對保護，不自動通過核對或發 LINE。
