# V87.4 縮圖、分頁與有上限的圖片快取

- 原圖不更動、不重新壓縮、不取代商品配對與發送使用的原檔 hash。
- 另建 WebP 預覽副本，最長邊 640 px、不放大、套用 EXIF 方向、透明背景白底，
  不複製 EXIF。每張最多 128 KiB；較複雜圖片降低副本品質／尺寸至限額內。
- 副本保存在同一專用 NAS 的 `thumbnails/v1/<prefix>/<thumbnail-sha256>.webp`。
  上傳 skip、不覆蓋、不刪除；重新讀回校驗後才發布獨立 `_商品縮圖位置`。
  索引以原圖 SHA-256 對應縮圖 SHA-256，包含版本、大小與固定圖庫，不存帳密。
- 由「NAS 縮圖管理」勾選讀取進度後，每次明確點擊建立最多 10 張。
  圖庫綁定及批次已保存的手選原圖皆納入；新圖片也可從此處補建。
  預覽沒有副作用，不在讀圖失敗時偷偷改抓原圖或改配對。
- 整批預覽每頁固定 6 款，上一頁／下一頁／頁碼可操作；含缺圖及暫緩列，
  69 款分 12 頁，最後一頁 3 款。未勾「載入本頁縮圖」不讀預覽圖片。
- 單款核對與下載仍用原檔。原檔快取最多 5 張／16 MiB；縮圖最多 48 張／8 MiB，
  按實際儲存的 base64 長度限額，LRU 淘汰。部署前遺留的大快取也會縮至上限。
  圖庫或設定狀態切換清除兩種快取；圖片 hash 與縮圖 hash 分開命名，不能誤用。
- 未配置 NAS 的其他部署保持原資料儲存，只按頁讀取舊圖產生暫時縮圖，不寫索引。
  NAS 已配置但縮圖未建立時，提示明確補建；已建立但損壞／離線／跨圖庫會報錯。

## 驗證

新增合成測試含 63 張／69 款、7 批建立、原雲表及原 NAS 逐項不變、重新開 store
只下載縮圖、63 張遍歷 12 頁無漏款、缺圖／暫緩、批次手選圖、上傳／索引逾時、
索引衝突、惡意來源／版本／路徑／大小、透明底／方向／尺寸／噪點圖、快取淘汰與
跨圖庫清除、UI 保留勾選、最後一頁邊界。縮圖專項含既有預覽共 34 項通過。

2026-09-15 完整回歸：`pytest -q -x`，823 項通過（42.15 秒）。

### 實際線上驗收（2026-09-15 已完成）

- `dce7597` 已部署原工具，頁首 V87.4 與 NAS 連線檢查通過均已確認。
- 63 張縮圖分 7 批（10／10／10／10／10／10／3）建立，每張寫後下載校驗再發布索引。
  介面顯示縮圖 63 張、待建立 0 張，建立按鈕停用；總計 2,647,614 bytes。
- 相較已驗證的原圖合計 13,899,638 bytes，縮圖副本總量約減少 81%；
  這是檔案大小比較，不是下載時間或整體效能的量測。
- 重開頁面後線上抽驗第 1、2、11、12 頁：第一頁第 1～6 款含 3 張可用縮圖及
  3 款待補圖；第二頁第 7～12 款 6 張；第 11 頁第 61～66 款 6 張；
  末頁第 67～69 款 3 張且下一頁停用。上一頁／下一頁／頁碼選取均實測成功。
- 上述縮圖皆成功解碼且長邊不超過 640 px；沒有讀取失敗。全部 12 頁逐項遍歷
  是本機 AppTest 的驗證範圍，不把四頁線上抽驗宣稱為線上全驗。
- 單款核對另實測原圖 1279×1706，原始／準備採用區均顯示原圖，未被縮圖替代。
- 原草稿仍為 69 款、63 款可帶圖、已選 0 款。原圖、報價、配對與人工核對未改，
  未發 LINE；NAS 縮圖副本建立不代表商品核對通過。

## 未包含

舊雲表 base64 原圖備份未刪除，整份 XLSX 匯出 403 限制仍未解決。
上傳檔案／待配對候選沿用原流程限額；本批快取上限針對已存圖庫的下載圖片，
不宣稱已限制所有 Streamlit 程式、Google API 回應或其他使用者的記憶體。
