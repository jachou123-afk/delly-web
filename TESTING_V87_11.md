# V87.11 第二次檢查與快速發送工作台

## 範圍與驗證

- 基於 GitHub main `ce877ade56795ec842b049ed5b7b00b006df04ce` 的獨立工作樹開發，保留原 checkout 未提交修改；沒有改供應商解析、價格規則、NAS 權限或正式業務資料。
- 解析／報價與原有審核維持原流程。快速工作台額外在發文前整批重讀來源、公式／報價依據、價格／單位／文案、同群歷史與原圖 SHA；不接受草稿、部分完成、結果待確認或未通過原核對規則的商品。
- 舊版明示接受的缺原文清單保持警告，不改成原文通過。選取正常商品可先準備，問題款不自動加入。
- 成功後使用 session 內凍結的原圖 ZIP／文案；每款不再讀 NAS、不重讀成本或報價表。下載／複製不產生任何發送紀錄。
- 完成按鍵是實際看到本款所有圖文相鄰出現的明示查驗，沿用 manual_visual 事件，不冒充 LINE API 回執；時間明示為登記時間，可另補 LINE 訊息時間。
- 保存先檢查本批版本鏈，再追加寫入並讀回；成功才換下一款。按鍵綁定執行 ID 與品號，延遲重按不會完成下一款。逾時即使伺服器已寫入也停下，不重送、不盲重試。
- `get_batch` 每次新讀 A:B 短索引及指定批次完整版本鏈，不讀其他批次 JSON。保留 checksum、分段完整性、parent 衝突、同 record_id 內容檢查及寫後驗證；頁面成功後只更新已保存批次快取。全歷史仍在開頁／第二次檢查時讀取供防重。

## 自動測試

使用 Streamlit 1.63.0 與合成 FakeSpreadsheet／合成圖片，沒有對真實 LINE 或雲表寫入。

- 新工作台 domain、UI 及 storage 組合：30 passed。
- 完整回歸：**924 passed、1 skipped，46.32 秒**。包含所有既有供應商解析、驗算、NAS 圖片、批次與 UI 測試；略過項目是未提供的私人圖片素材。
- 涵蓋唯讀準備、缺少查驗聲明、草稿、部分圖文、結果不明、來源／公式／原圖變動、審核證據缺失、缺原文明示接受、錯款完成、未確認卻點完成、成功後下一款、不重複成本／圖片讀取、暫停／版本失效、其他裝置更新、只選正常款、保存後逾時、防重登及短索引讀取完整性。

## 限制

- 不含 LINE API、自動按傳送或新的發送授權；仍由使用者或已授權 Computer Use 操作 LINE 可見介面。
- 第二次檢查是當時快照，不是監控雲表的背景服務。知道來源／內容變更時必須暫停重查；不為追求速度隱藏變更。
- 工作台不是跨機發送鎖。同一批仍須單一執行者，崩潰或換機須先重讀雲表、LINE 與工作紀錄，不直接從「待發」推定未發。
- 一輪原圖上限仍為 64 MB；首次備圖仍受 NAS 速度影響。未進行真實 LINE 發送速度量測，不承諾固定秒數或加速倍數。
- 不自動結案；歷史舊圖／重複等異常仍保留原本的結案阻擋。

## 部署

本版只更新功能程式、合成測試、SOP 及此功能說明；歷史交接文件不在本次上傳範圍。待提交部署後，以正式站頁首及工作台入口做唯讀驗收，不以真實商品測試完成按鍵；部署结果在本節更新。
