# V87.17 商品資料修正後沿用同款原圖

當型號或品名修正後，原本的圖片配對仍會先失效，避免自動把舊圖套到不同商品。

若操作者已逐張核對並確認仍是同一款商品，單款圖片區會顯示變更前已綁定的原圖；必須勾選確認，才可按「沿用舊圖並更新本商品配對」。系統沿用原圖的 SHA-256 與原始位元，不重新猜圖，也不標記商品已核對、不批准批次、不發 LINE。

驗證：

- `tests/test_product_image_ui.py`：7 passed。
- `tests/test_product_image_library.py` 與圖片 UI 合跑：26 passed，1 次既有 15 秒載入測試逾時；該逾時案例與新增案例單獨重跑均通過。
- `compileall` 與 `git diff --check` 通過。
