# 交接狀態

## 最新上線版：V82 發送管理（2026-09-14）

- 部署狀態：已上線。三批提交 `f1f12d5`、`cfc7ed6`、`6ed60db` 已一起推送至原 GitHub `main`；部署基底為 `f15dc329c8c5d6bfe7bd0ed84bda0e3de01a0c58`，未覆蓋其他遠端變更。
- 正式站讀取驗收：原 Streamlit 網址已顯示 V82，「發送管理」可開啟、可載入報價表；9/12 篩選穩定後列出 68 款，包含 1127、1128、1129。僅讀取與切換篩選，未建立真實批次、未發 LINE、未改商品列。部分舊商品顯示計價單位待核對，未自動修正或宣稱全批資料通過。
- 驗收補強：日期改變時，自動產生的批次名稱同步更新；使用者自行輸入的名稱保持不變。新增真實 Streamlit 元件回歸測試，部署前完整合計 438 項通過。
- 完整測試已通過 437 項（22.46 秒）；`git diff --check` 無錯誤。首次 GitHub 更新因單次超過 200000-byte 安全審查上限被拒絕，未改正式站。使用者已明確同意「分批更新」；本輪採完整檔案分批審查，所有檔案備齊後才一次更新 main，避免正式站混用新舊版本。未改正式雲表、未發送 LINE。
- 分批 GitHub 連線已不再觸發大小限制，但 Git Trees 寫入回傳 403。已修正本機 Git 的 helper 路徑並以單次 OpenSSL 設定保持 HTTPS 憑證驗證；使用既有 Git 登入，`push --dry-run` 已通過，未變更全域 Git 設定或帳號權限。改用分批本機提交、完整版本一次推送。
- 在原 `dolly_parser.py` 加入「報價整理／發送管理」頁籤；發送頁獨立於成本設定載入，既有供應商解析與成本公式不變。
- 新模組：`dispatch_manager.py`（批次、確認、狀態與對帳）、`dispatch_storage.py`（專用雲表追加紀錄與原圖）、`dispatch_ui.py`（操作頁面）。
- 批次固定商品清單、目標聊天室、圖文版本與排除原因；確認時重讀來源及圖檔，之後鎖定內容。圖片與文案分別人工查驗登記，不把產生文案或點擊按鈕當成 LINE 回執。
- 既有私人圖片、LINE 歷史及 9/12 商品不會自動匯入或標記已發；首次使用需加入原圖並核對歷史。程式無 LINE 傳送 API。
- 儲存草稿／圖片時才會建立 `_發送批次`、`_發送圖片`；原商品列與公式不改写，既有商品讀取略過上述紀錄表。
- 本機回歸與隔離的雲表替身測試通過；已在瀏覽器用合成資料驗證圖文卡片、批次確認與 0/5 待發狀態。完整範圍及限制見 `TESTING_V82.md`。
- V82 包含下列先前 V81 本機共用防呆；部署前以遠端 `main` 最新版本比對，保留既有工作內容。

## V81 本機候選版紀錄（已包含於 V82，以下為歷史）

- 工作分支：`fix-v80-unit-price-name`；本輪修改尚未提交／推送／部署，正式網站版本未在本輪確認。
- 共用防呆：換原文重設草稿並重選廠商／分類；廣告輸出前先檢查成本／報價一致性，再人工核對，產生時重讀雲表確認未變更。
- 沒有改寫既有來源解析、廠商包郵判斷、成本公式或雲表寫入函式；另修正文案混合欄位的誤刪／洩漏。
- 本機測試 394 項通過。優娜卡樂星與其他未提供的真實來源格式尚未驗收，不能宣称全部廠商通過。
- 完整驗證、舊資料阻擋行為與尚未解決風險見 [TESTING_V81.md](TESTING_V81.md)。本輪未改雲表、未發 LINE、未重新核對 9/12 全批商品。

## 以下為前次 V80 交接紀錄（歷史）

- 日期：2026-09-14（Asia/Taipei）
- 正式分支：`main`
- 目前版本：V80
- 本次修改：LINE 文案移除「新品」字樣；來源明確標示正版時，固定以獨立的「正版授權」首行呈現；若有彩盒／包裝／端盒尺寸則不再顯示產品尺寸。
- 代號規則：固定為 `BGD-分頁代號-NO數字`，目前只有已確認的 `G正版 → G`、`S生活用品 → S`；未知分頁一律停止，不猜代號。
- 廣告排除規則：重量、外箱尺寸、木架／木框及無木架成本備註不對外顯示；有包裝尺寸時排除產品尺寸；相同的包裝尺寸只顯示一次並優先採用彩盒尺寸。包裝方式、材質、顏色與圖案等仍保留，且包裝方式不會觸發尺寸排除。
- 價格與單位：售價取雲表 10% 報價無條件進位；計價單位與裝箱單位必須一致，例如 K-8141 顯示 `售價61元/盒`，不可固定寫成每個。
- 安全邊界：上述排除只作用於廣告輸出；採購解析、成本計算、雲表重量、外箱尺寸與木架稽核原文均維持完整。10% 報價、NO、單位或商品名稱缺失／不合法時停止產生文案。
- 本機驗證：`pytest -q -x` 通過 116 項；包含正版置頂、新品移除、包裝尺寸優先、同值尺寸去重、包裝方式不誤觸發，以及既有解析、成本防呆與雲表流程。
- 前版 V78 的行內箱重解析與防呆規則保持不變。
- 部署驗收：程式推送至 `main` 後，仍須確認正式網站顯示 V80，並實測 LINE 廣告文案預覽。
