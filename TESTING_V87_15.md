# V87.15 問題款逐欄修正

## 使用方式

發送管理 → 選既有草稿 → 在「③ 發送前問題與修正」點問題商品 → 展開的單款明細按「修正本款原雲表（逐欄）」。

1. 以既有商品 ID、分頁、列位置及来源版本固定目標，重新查唯一性。重複、刪除、搬移或資料變動會停止，不自行猜測另一筆。
2. 貼本款原文並解析。沿用報價整理同一個解析器，不另外發明廠商格式。解析結果只作建議，顯示原表值與解析值；所有欄位預設不選。
3. 選要修正的欄位，輸入修正值、操作者及依據，再預覽。可修品名、廠商貨號、進價、裝箱、計價單位、整箱／單位重量、三類尺寸及其餘商品資訊。品號、原日期、供應商、費率／匯率、圖片及格式不在本入口任意改寫範圍。
4. 品名／貨號／尺寸／備註不重算價格；只有選算價欄位才顯示完整連動成本、四種報價及廣告售價。必須有原來源／公式／規則版本相符的已存報價依據，沿用其中匯率、內陸及國際費率，不讀取當天側欄預設值。缺參數先至既有「補存／修正原文」核實保存，再重新載入修正。
5. 改計價單位必須一併勾選核實進價及每箱數量，即使數字不變；不以改單位名稱冒充換算。重複尺寸標籤不自行合併刪除，備註不得偷渡尺寸或單位欄位。
6. 預覽後鎖定內容，明確勾選同商品、選定變更與連動價格同意後才能保存；返回修改會重新產生預覽。未勾選欄位不寫，只有必要連動公式／單位標頭同步。

## 保存與防護

- 保存前核實雲表 ID、分頁 ID、原 NO 唯一性、行位置、來源與公式快照、原依據版本；重新建立修正計畫及跨商品重複檢查，送出前再查一次。文字以 stringValue 保存，不能以等號開头內容注入公式。
- 同一 Google Sheets batchUpdate 同時寫入選定差異格與 `_商品修正` 的分段校驗紀錄，保留時間、操作者、依據、原文、解析建議、原值、新值及操作識別。只更新 userEnteredValue，不更動格式、圖片或保護欄位。查看／解析／預覽不建立紀錄表，不寫商品。
- 寫後核對全部六列公式／保留欄位及連動數值，不以 HTTP 成功代替讀回。相同操作識別先查既有結果，不重複新增歷史。結果不明時只提供查詢及備份，不自動重送；回清單仍會阻擋含未釐清款的批次確認／工作台。
- 成功後清除受影響驗算快取及來源目錄，草稿顯示來源已變動。由既有「更新本款來源與文案」帶入，再重新验算／核對圖片及原文；舊已確認批次／回執不改、既有問題不自動解除、不自動核准或發 LINE。
- 舊 `_報價依據` 保留歷史，不自動當作新版全欄位原文已核。新原文與修改依據保存在修正紀錄；需要新版完整報價依據時仍經原本補存核對流程，不偽造原文已核狀態。

## 限制及驗證

- Google 保證同一 batchUpdate 內的更新一起套用，但不是與外部人工編輯之間的比較交換鎖；最後讀取與送出之間仍有極短競態窗口。此版採送前／送後比對，不宣稱能絕對排除同時直接編輯雲表的覆寫。多人同時修同一款應避免。依據：[Google Sheets batchUpdate](https://developers.google.com/workspace/sheets/api/reference/rest/v4/spreadsheets/batchUpdate)。
- 未確認操作的備份仍為 session-local，可下載；不能宣稱重新整理／關閉後能自動恢復所有未送達請求。正式修正歷史只有 Google 已接受的原子更新才存在。
- 33 個新增合成測試通過：逐欄保留、參數缺失／過期、成本連動、其他廠商運費規則、單位換算確認、來源／公式／目的表／分頁漂移、重複品號與貨號、送前變動、寫後不一致、逾時查詢、防重及 UI 完整流程。
- 完整回歸：993 passed、1 skipped（51.43 秒）。
- 2026-09-18 正式站正常重新啟動後顯示 V87.15；既有草稿的單款明細出現逐欄修正與歷史按鈕，實際點開成功讀取唯一商品快照，顯示固定目標、原文欄位及不寫入解析入口，再不保存關閉。正式站僅做只讀驗收，未修改真實商品、未解除問題、未核准或發 LINE；保存／逾時／衝突流程以合成資料測試，不冒充正式商品寫入驗收。
