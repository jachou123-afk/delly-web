# V87.2 NAS 一次性連線檢查

僅加入部署驗證；`CloudDispatchStore`、商品圖庫索引、原圖匯入、報價、
人工核對與 LINE 邏輯均未切換或修改。完整遷移與縮圖仍是後續階段。

## 私密設定與執行邊界

- 只取 `st.secrets["nas_images"]` 的 base_url、root、library_id、account、password。
  公開程式碼、回執、UI、雲表不保存真實主機／帳密／SID。
- 沒有設定不連線，無效／未替換占位設定不登入。
- 每個 Python 程式啟動只檢查一次；鎖保護跨使用者併發，成功和失敗皆保留。
  不使用使用者可控制的參數，不提供公開重試／NAS 路徑／檔案輸入按鈕。
- 只有部署、人工重啟等新程式啟動才可能再次檢查；失敗先查原因，不反覆重啟。
- 固定 8×8 合成 PNG，小於 1 KB，存到已授權專用圖庫的 content-addressed
  originals 路徑。不覆蓋，不刪除，不發布商品圖片索引；同圖不累積副本。
- 先登入、讀精確根資料夾、上傳並校驗，再登出後使用新連線登入讀回比對。
- 任一階段失败即停止，回執只有固定階段、遮蔽原因、時間及測試圖大小／雜湊。
  例外不顯示原始訊息／堆疊，不影響原報價頁渲染。
- 原圖儲存元件仍強制 TLS、禁止轉向、限制回應大小、不重試帳密。

## 驗證

新增測試涵蓋真實 adapter 配合合成 HTTP 的兩次登入與固定圖往返、設定無效
不碰網路、各阶段失败、例外遮蔽、48 次併發只執行一次，以及真正 Streamlit
元件沒有公開輸入／重試、重新渲染不重跑。所有測試只使用假帳密與合成資料。

2026-09-15：NAS 元件與連線檢查合計 68 項測試通過（8.72 秒）；
完整回歸 **764 項通過（80.18 秒）**，包含既有報價、各廠商格式、
圖片配對、多選／全選、人工核對、待發清單與發送後對帳測試。

部署後須以線上 V87.2 的回執為實測依據；本機測試通過不代表 NAS 已連通，
NAS 通過亦不代表商品圖已搬移。若失敗，保留安全限制並停止切換。
