# V87.13 驗算保存與既有問題带入

## 範圍

- 驗算結果寫入既有 `_發送批次` 的 append-only 紀錄，讀回成功才顯示保存完成。包含各款時間、規則、來源／公式／原文參數版本與結果。部分重算保留其他款結果，不自動核准。
- 重開批次依版本比對恢復結果，不重新計算；只有變動／無法核實的款失效，失效原因留在表內。新工作階段會查版本，連續 UI 操作不反覆重查。不是背景監控；確認／準備圖文前的第二次檢查仍保留。
- 既有問題透過正常 UI 預覽、保存到獨立 `_核對問題`。以品號身分＋供應商＋來源日期關聯相關批次，不依相似名稱配對。數學通過不清除商品問題。
- 問題列含欄位、差異、原紀錄、核對依據、證據及下一步；未解決問題阻擋確認及第二次檢查。已解決需明确勾選、操作者及實際證據，保存時間與來源版本；保留歷史，不刪除。登記解除是操作者聲明，不是程式自行證明商品已修好。
- JSON 匯入限本批唯一品號與必要文字欄，不接受已解決／通過狀態；同內容重複匯入不重建、不重開已解除問題。版本衝突或讀取不完整停止確認，不覆蓋。
- 不改商品報價、不新增商品列、不發 LINE，不繞過原圖／單位／來源／防重／第二次檢查。真實商品資料及匯入紀錄只留私有來源與原雲表，不放 GitHub。

## 驗證

- 本機完整回歸：950 passed、1 skipped（48.94 秒），包含 UI 匯入、重新載入、驗算後保留問題、解除確認與證據測試。
- 覆蓋新工作階段恢復、時間不變、單款來源／公式／依據變更、規則變更、讀取與保存失敗、部分重算、並行衝突、跨批帶入、供應商／日期隔離、重複匯入、解除證據、原資料不變及無自動核准。
- 正式站驗收待部署後進行；本文件不把本機測試當成上線證據。
