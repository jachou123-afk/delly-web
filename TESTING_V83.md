# V83 可操作的逐款核對工作台

## 本次修正

- 全批清單改為順序、品號、商品、售價、單位、圖片、核對狀態的短欄位；可搜尋及點選單列，詳細問題不再擠在單格。
- 選中商品後左右對照原報價表資料／原圖與待發圖片／可編輯文案；另列品號、品名、10% 報價轉售價、計價單位、裝箱及交期的逐欄對照表。
- 原始資料明確標示為「報價表保存內容」，不是完整廠商聊天原文；原表链接直接定位六列商品區塊，原圖可另行目視比對。
- 缺少固定「計價單位」欄位的舊資料，僅依明確裝箱資料產生待確認預覽。核對人確認售價與裝箱採相同單位並填依據後，才可勾選核對。顯式單位衝突或成本不一致仍阻擋；不改原報價表，不放寬原 LINE 文案功能的嚴格檢查。
- 文案因來源錯誤未能產生時，只提示根本原因及下一步，不再連帶報品號／售價／裝箱錯誤。
- 「確認並下一款」保存本款並選到下一個未完成商品；修改圖片、文案、單位證據後重新核對。來源變更時阻擋單款確認與整批確認。
- V82 草稿在記憶體內補入同一來源的對照資料，保留人工改過的文案、原圖、排除原因與備註；直到使用者儲存才寫入修訂。已確認／已發批次完全不遷移，原有凍結校驗保持相容。

## 圖片來源與限制

- 透過現有 Google 連線唯讀匯出 Excel，從內嵌圖與工作表錨點提出候選。只接受同分頁、品號與品名皆相符，且錨點在本商品六列的 M:T 範圍內。候選不是語意識別，仍需操作者核對。
- Google 官方匯出 API 有 10 MB 上限；大報價表可能无法匯出。畫面提供 ZIP 原圖包／Excel 匯入，不將匯出失敗解讀為沒有圖片。
- ZIP 只按完整貨號或 BGD 品號配對，不作相近檔名猜測；相同貨號對應多款時保持未配對。圖片先在工作階段預覽，儲存本款時才保存採用的圖片。
- 圖片原位元保留、不壓縮；單張 2 MB、每款 5 張。外部圖片關聯不下載；ZIP 不解壓到磁碟，限制壓縮／展開容量、檔案數與 XML 實體。
- 本機已有的 65 張原圖另整理為工作區 ZIP，未加入公開程式庫、未自動上傳正式管理區。這不代表 68 款全部都有唯一配對圖片。

## 驗證

- 完整測試 477 項通過；原有 438 項全部保留。
- 全部既有測試加上舊格式多供應商／7 種單位、來源衝突、空文案不連鎖誤報、V82 草稿補資料、已確認批次校驗相容、圖片錨點與檔名唯一性，以及 Streamlit 實際核對／下一款流程回歸。
- 使用記憶體 Google Sheets 替身驗證保存核對不改原商品列，不自行增加 LINE 發送狀態。
- 瀏覽器以本機合成商品演練。正式站驗收僅讀取既有批次和商品、選擇核對項目，不替使用者勾選、儲存核對或發 LINE。

## API 依據

- [gspread 6.2.1 Spreadsheet.export](https://docs.gspread.org/en/v6.2.1/api/models/spreadsheet.html#gspread.spreadsheet.Spreadsheet.export)
- [Google Drive files.export 與 10 MB 上限](https://developers.google.com/workspace/drive/api/reference/rest/v3/files/export)
- [Streamlit dataframe 的單列選取](https://docs.streamlit.io/develop/api-reference/data/st.dataframe)
